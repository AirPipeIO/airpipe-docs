---
sidebar_position: 3
slug: /configuration/interfaces
displayed_sidebar: configurationSidebar
---

# Interfaces

:::tip Full field reference
Every field with types, defaults and examples lives in the
**[Interfaces reference](/docs/reference/interfaces)**, generated from the engine schema.
:::

Add any number of interfaces to a single configuration to interact with. Interfaces run a number of **actions** when triggered.

## Defining Interfaces

Defining interfaces are simple. Declare an `interfaces` section and create your identifiers for each interface.

<h3>Usage</h3>

```yml
name: get-vault-secrets

interfaces:
  prod/user-login:
  prod/user-details:
```

## Common options

`summary`, `description`, `method`, `route`, `tags`, `output`, `actions`,
`schedule`, `assert`, `response`, `mcp`, `network` and more — the full list with
types and defaults is in the [Interfaces reference](/docs/reference/interfaces).

<h3>Usage</h3>

```yml
name: get-vault-secrets

interfaces:
  prod/user-login:
    summary: production endpoint
    description: production endpoint for login service
    tags: ['env:production', 'version:1.0.0']
    output: http
    method: POST

    actions:
      - name: CheckInput
      - name: VerifyUser
      - name: VerifyPassword

```
